{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "start:back": "pnpm --filter @workspace/api-server run dev",
    "start:front": "pnpm --filter @workspace/colorgifts run dev",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "build:front": "pnpm run typecheck:libs && pnpm --filter @workspace/colorgifts run build && rm -rf dist && cp -R artifacts/colorgifts/dist/public dist",
    "build:back": "pnpm run typecheck:libs && pnpm --filter @workspace/api-server run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["sharp"]
  }
}
